server:
  port: 8081
  tomcat:
    accesslog:
      enabled: true
      directory: "/tmp/service-template"
      prefix: stdout
      buffered: false
      suffix:
      file-date-format:
      pattern: "[ACCESS] %{org.apache.catalina.AccessLog.RemoteAddress %l %t %D %F"

consul:
  host: int-consul.adnear.net:80